# 配置数据连接
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://northsailor.mysql.rds.aliyuncs.com:3306/tmooc_work?useUnicode=true&characterEncoding=utf8&useSSL=false
    platform: mysql
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: Hjq910314
    show-sql: true
    database: mysql
  mvc:
    static-path-pattern: /static/**
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    open-in-view: true
  redis:
    host: 39.107.98.120
    port: 6379
    pool:
      max-active: 8
      max-idle: 8
      max-wait: -1
      min-idle: 0
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    content-type: text/html
    enabled: true
    encoding: UTF-8
    mode: HTML5
  resources:
    add-mappings: true
    cache-period: 3600
    static-locations: classpath:/static/
    chain:
      cache: true
      enabled: true
      gzipped: true
      html-application-cache: true
server:
  port: 8080
